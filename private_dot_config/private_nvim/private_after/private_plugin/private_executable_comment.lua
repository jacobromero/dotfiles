local comment = require('Comment')
comment.setup({
    toggler = {
        line = 'gcc',
    },
    opleader = {
        line = 'gcc',
    }
})

-- vim.keymap.set('v', '<C-/>', '<Plug>(comment_toggle_linewise)', {})
